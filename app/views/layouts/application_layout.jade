!!! 5
html(lang="en")
  head
    include _head
  body
    include _top_navbar

    .container
      include _flash

      if contentFor('header').length > 0
        .page-header
          h1
            != contentFor('header')

      != body

      hr
      footer
        p © Your Co #{new Date().getFullYear()}
